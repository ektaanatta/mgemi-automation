import { test, expect } from '@playwright/test';

test('Correct login', async ({ page }) => {
    // await page.goto('https://mgemi.com/account/login');
    // // await page.pause()
    // await page.click("[id='CustomerEmail']");
    // await page.locator("[id='CustomerEmail']").fill('kavita@anattadesign.com');
    // const elementText = await page.evaluate(() => {
    //     const shadowHost = document.querySelector('div[data-nametag="shop-portal-provider"]');
    //     const shadowRoot = shadowHost.shadowRoot;
    //     const targetElement = shadowRoot.querySelector('section.authorize-modal');
    //     return targetElement.textContent;
    //   });
    //   console.log(elementText);
    // await page.getByTestId('authorize-modal-close-button').click();
    // await page.getByRole('textbox', { name: 'Password' }).click();
    // await page.getByRole('textbox', { name: 'Password' }).fill('Kavita@29');
    // await page.getByRole('button', { name: 'Log in' }).click();
    // await expect(page).toHaveURL("https://mgemi.com/account");
    // await expect(page.getByText('My account')).toBeVisible();

    // await page.click("[id='data-testid']");
    
});
